# GitHub package consumer

Run `craft install` before `craft check`, `craft test`, `craft run` or `craft build`.
Expected run output: `Hello, Craft!`. This downloads source from spidermeaow/craft-test at v1.0.0; it never runs an installation hook. See ../../docs/TRY-REV11.md.
